import styled from "styled-components"

const StyledForm = styled.form`
  display: flex;
  flex-direction: column;

  input {
    margin: 2rem 0;
    padding: 1rem;
  }

  textarea {
    padding: 2rem 0;
  }

  button {
    margin: 2rem 0;
    padding: 1rem;
  }
`

export { StyledForm }
